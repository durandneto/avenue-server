This directory contains the server code necessary to run the api to Avenue Test

1. Run `npm install`
2. Run `npm test && npm run test:api && nodemon server.js` to run all tests and create a server at port 3030
